aop
静态代理：
        每次访问目标对象都通过代理对象进行访问
        目标对象有的方法也要在代理对象中实现
        通过代理对象控制目标对象执行的过程 加入额外操作 达成代码增强的效果
        一对一：只针对一个类进行代理 在代理类中需声明目标对象(接口和实现类都可以)
缺点：不具有灵活性

动态代理：
        两种：
            1.jdk动态代理
                        要求：必须有接口，代理类和目标类实现同一接口，最终生成的代理类在com.sun.proxy包下，类名为$proxy数字(eg：$proxy2)
            2.cglib动态代理
                                      最终生成代理类会继承目标类，并和目标类在相同包下
