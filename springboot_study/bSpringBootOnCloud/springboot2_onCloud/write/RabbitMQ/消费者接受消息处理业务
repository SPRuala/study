构建消费者处理消息、监听队列:
       ·创建RabbitMQConsumer.java,作为组件存在于容器中(@Component)
       ·创建监听队列
            根据获取到的不同routingKey做不同的业务处理(类似于controller的请求映射)

生产者、消费者可存在于不同项目之中(跨项目存在),该特性体现为解耦
消费者处理掉消息后,RabbitMQ管理页中的消息都会清空